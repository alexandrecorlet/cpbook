/*
 * Kattis - Solving for Carrots
 * author: alexandrecorlet
 */

#include <bits/stdc++.h>

using namespace std;

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n, p;
    cin >> n >> p;
    while (n--) { 
        string description; 
        cin >> description; 
    }
    cout << p << '\n';
    return 0;
}
