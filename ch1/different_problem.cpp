/*
 * Kattis - A Different Problem
 * author: Alexandre Corlet
 */

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int32_t main() {
    ll x, y;
    while (cin >> x >> y)
        cout << abs(x - y) << '\n';
    return 0;
}
